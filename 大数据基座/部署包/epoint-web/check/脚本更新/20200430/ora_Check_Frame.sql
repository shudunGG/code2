-- 这里是检测每天最后一条数据是否存在，所以第一个人建立的人请把最下方的数据做一个check
select count(*) from USER_IND_COLUMNS WHERE table_name =upper( 'frame_userrolerelation') and index_name=UPPER('UQ_FRAME_USERROLERELATION') and column_name=upper('ouguid')